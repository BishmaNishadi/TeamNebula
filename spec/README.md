# Place spec files here

Put PDF, TXT, or MD spec files in this folder. The Copilot agent will read them in Phase 1.

If this folder is empty, the agent will live-explore the target URL (configured in `automation/playwright.config.ts`) to derive test cases.
